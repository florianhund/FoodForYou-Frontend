import {
  getAllMeals,
  getMealById,
  postMeal,
  updateMeal,
  deleteMeal
} from './meal';
import { signIn, signOut } from './auth';
import { getUserByEmail, createUser } from './user';

export {
  getAllMeals,
  getMealById,
  postMeal,
  updateMeal,
  deleteMeal,
  signIn,
  signOut,
  getUserByEmail,
  createUser
};
